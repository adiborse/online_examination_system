<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="icon" type="image/x-icon" href="/favicon.ico">
</head>
<body>
    <header class="header">
        <div class="container">
            <div class="logo">
                <a href="/" style="color: white; text-decoration: none;">📚 Online Exam System</a>
            </div>
            <nav>
                <ul class="nav-links">
                    <% if (user) { %>
                        <li><span>Welcome, <%= user.name %>!</span></li>
                        <% if (user.role === 'admin') { %>
                            <li><a href="/admin/dashboard">Dashboard</a></li>
                            <li><a href="/admin/questions">Questions</a></li>
                            <li><a href="/admin/results">Results</a></li>
                        <% } else { %>
                            <li><a href="/exam/dashboard">Dashboard</a></li>
                        <% } %>
                        <li><a href="/logout">Logout</a></li>
                    <% } else { %>
                        <li><a href="/login">Login</a></li>
                        <li><a href="/register">Register</a></li>
                    <% } %>
                </ul>
            </nav>
        </div>
    </header>
    <main class="main-content">
