<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/auth.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
</head>
<body class="role-body">
    <div class="auth-background">
        <div class="auth-container">
            <div class="auth-card role-card">
                <div class="auth-header">
                    <div class="icon-wrapper main-icon">
                        <i class="fas fa-graduation-cap"></i>
                    </div>
                    <h1>Online Exam System</h1>
                    <p>Choose your role to continue</p>
                </div>

                <% if (error) { %>
                    <div class="error-message">
                        <i class="fas fa-exclamation-circle"></i>
                        <%= error %>
                    </div>
                <% } %>

                <div class="role-selection">
                    <div class="role-option admin-option">
                        <div class="role-icon-wrapper admin-gradient">
                            <i class="fas fa-user-shield"></i>
                        </div>
                        <div class="role-content">
                            <h3>Administrator</h3>
                            <p>Manage exams, questions, and view results</p>
                            <ul class="role-features">
                                <li><i class="fas fa-check"></i> Create & manage questions</li>
                                <li><i class="fas fa-check"></i> Monitor exam sessions</li>
                                <li><i class="fas fa-check"></i> View detailed reports</li>
                            </ul>
                        </div>
                        <a href="/admin/login" class="btn btn-admin">
                            <i class="fas fa-sign-in-alt"></i>
                            Admin Login
                        </a>
                    </div>

                    <div class="role-option student-option">
                        <div class="role-icon-wrapper student-gradient">
                            <i class="fas fa-user-graduate"></i>
                        </div>
                        <div class="role-content">
                            <h3>Student</h3>
                            <p>Take exams and view your results</p>
                            <ul class="role-features">
                                <li><i class="fas fa-check"></i> Take timed exams</li>
                                <li><i class="fas fa-check"></i> View instant results</li>
                                <li><i class="fas fa-check"></i> Track your progress</li>
                            </ul>
                        </div>
                        <a href="/student/login" class="btn btn-student">
                            <i class="fas fa-sign-in-alt"></i>
                            Student Login
                        </a>
                    </div>
                </div>

                <div class="auth-footer">
                    <div class="footer-links">
                        <p>Don't have an account? <a href="/register" class="link-primary">Register here</a></p>
                        <p><a href="/" class="link-secondary">Back to Home</a></p>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body.role-body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 50%, #f093fb 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
        }

        .auth-background {
            position: relative;
            width: 100%;
            max-width: 900px;
        }

        .auth-background::before {
            content: '';
            position: absolute;
            top: -20px;
            left: -20px;
            right: -20px;
            bottom: -20px;
            background: linear-gradient(45deg, rgba(255,255,255,0.1), rgba(255,255,255,0.05));
            border-radius: 30px;
            z-index: -1;
        }

        .auth-card.role-card {
            background: rgba(255, 255, 255, 0.98);
            padding: 50px;
            border-radius: 25px;
            box-shadow: 
                0 25px 80px rgba(0, 0, 0, 0.15),
                0 10px 40px rgba(102, 126, 234, 0.2);
            backdrop-filter: blur(20px);
            border: 1px solid rgba(255, 255, 255, 0.2);
            position: relative;
            overflow: hidden;
        }

        .auth-card.role-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 4px;
            background: linear-gradient(90deg, #667eea, #764ba2, #f093fb);
        }

        .auth-header {
            text-align: center;
            margin-bottom: 50px;
        }

        .icon-wrapper.main-icon {
            width: 100px;
            height: 100px;
            margin: 0 auto 25px;
            background: linear-gradient(135deg, #667eea, #764ba2);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            box-shadow: 0 12px 35px rgba(102, 126, 234, 0.3);
        }

        .icon-wrapper i {
            font-size: 3rem;
            color: white;
        }

        .auth-header h1 {
            color: #2c3e50;
            font-size: 2.5rem;
            font-weight: 700;
            margin-bottom: 12px;
            letter-spacing: -0.5px;
        }

        .auth-header p {
            color: #64748b;
            font-size: 1.1rem;
            font-weight: 400;
        }

        .error-message {
            background: linear-gradient(135deg, #ff6b6b, #ee5a52);
            color: white;
            padding: 16px 20px;
            border-radius: 12px;
            margin-bottom: 30px;
            display: flex;
            align-items: center;
            gap: 12px;
            font-weight: 500;
            box-shadow: 0 4px 15px rgba(255, 107, 107, 0.3);
        }

        .role-selection {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 40px;
            margin-bottom: 40px;
        }

        .role-option {
            background: white;
            padding: 40px 30px;
            border-radius: 20px;
            text-align: center;
            transition: all 0.4s ease;
            border: 2px solid transparent;
            position: relative;
            box-shadow: 0 8px 30px rgba(0, 0, 0, 0.1);
        }

        .role-option:hover {
            transform: translateY(-8px);
            box-shadow: 0 20px 50px rgba(0, 0, 0, 0.15);
        }

        .admin-option:hover {
            border-color: #11998e;
            box-shadow: 0 20px 50px rgba(17, 153, 142, 0.2);
        }

        .student-option:hover {
            border-color: #667eea;
            box-shadow: 0 20px 50px rgba(102, 126, 234, 0.2);
        }

        .role-icon-wrapper {
            width: 80px;
            height: 80px;
            margin: 0 auto 25px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.15);
        }

        .admin-gradient {
            background: linear-gradient(135deg, #11998e, #38ef7d);
        }

        .student-gradient {
            background: linear-gradient(135deg, #667eea, #764ba2);
        }

        .role-icon-wrapper i {
            font-size: 2.2rem;
            color: white;
        }

        .role-content h3 {
            color: #2c3e50;
            font-size: 1.5rem;
            font-weight: 700;
            margin-bottom: 12px;
        }

        .role-content > p {
            color: #64748b;
            font-size: 1rem;
            margin-bottom: 25px;
        }

        .role-features {
            list-style: none;
            text-align: left;
            margin-bottom: 30px;
        }

        .role-features li {
            display: flex;
            align-items: center;
            gap: 12px;
            margin-bottom: 10px;
            color: #4b5563;
            font-size: 0.95rem;
        }

        .role-features i {
            color: #10b981;
            font-size: 0.8rem;
            width: 16px;
        }

        .btn {
            font-family: inherit;
            font-size: 1rem;
            font-weight: 600;
            padding: 16px 32px;
            border: none;
            border-radius: 12px;
            cursor: pointer;
            transition: all 0.3s ease;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            gap: 10px;
            text-decoration: none;
            width: 100%;
        }

        .btn-admin {
            background: linear-gradient(135deg, #11998e 0%, #38ef7d 100%);
            color: white;
            box-shadow: 0 4px 15px rgba(17, 153, 142, 0.4);
        }

        .btn-admin:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(17, 153, 142, 0.4);
        }

        .btn-student {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            box-shadow: 0 4px 15px rgba(102, 126, 234, 0.4);
        }

        .btn-student:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(102, 126, 234, 0.4);
        }

        .auth-footer {
            text-align: center;
            margin-top: 40px;
        }

        .footer-links p {
            margin-bottom: 12px;
            color: #64748b;
            font-size: 1rem;
        }

        .link-primary {
            color: #667eea;
            text-decoration: none;
            font-weight: 600;
            transition: all 0.2s ease;
        }

        .link-primary:hover {
            color: #5a6fd8;
            text-decoration: underline;
        }

        .link-secondary {
            color: #94a3b8;
            text-decoration: none;
            font-weight: 500;
            transition: all 0.2s ease;
        }

        .link-secondary:hover {
            color: #667eea;
            text-decoration: underline;
        }

        @media (max-width: 768px) {
            .role-selection {
                grid-template-columns: 1fr;
                gap: 25px;
            }

            .auth-card.role-card {
                padding: 30px 25px;
                margin: 10px;
            }

            .auth-header h1 {
                font-size: 2rem;
            }

            .icon-wrapper.main-icon {
                width: 80px;
                height: 80px;
            }

            .icon-wrapper i {
                font-size: 2.5rem;
            }

            .role-option {
                padding: 30px 25px;
            }
        }

        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .auth-card.role-card {
            animation: fadeInUp 0.6s ease-out;
        }

        .role-option {
            animation: fadeInUp 0.6s ease-out;
        }

        .admin-option {
            animation-delay: 0.1s;
        }

        .student-option {
            animation-delay: 0.2s;
        }
    </style>
</body>
</html>
